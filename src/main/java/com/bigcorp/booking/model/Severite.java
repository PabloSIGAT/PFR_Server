package com.bigcorp.booking.model;

/**
 * Attention, enum persistée en base, 
 * ne pas modifier les valeurs énumérées (à moins de faire 
 * passer un script SQL de modification de base auparavant).
 */
public enum Severite {

	 PAS_GRAVE, GRAVE;
	
}
